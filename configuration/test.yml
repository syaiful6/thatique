version: 0.1
log:
  level: info
  fields:
	environment: test
http:
  addr: localhost
  headers:
	X-Content-Type-Options: [nosniff]
redis:
  addr: localhost
  password: secret
  db: 1
mongodb:
  uri: "mongodb://localhost:2701"